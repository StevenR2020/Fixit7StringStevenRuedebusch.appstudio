// This line of code declares the variable with the string and prints it
let quoteString = ["Failure is simply the opportunity to begin again this time more intelligently"]
alert(quoteString)

// This line of code changes the quoteString variable string to uppercase and prints it
let upperCaseString = quoteString.toUpperCase();
alert(`Upper case string is: ${upperCaseString}`);

// This line of code creates the string, "-Henry Ford".
let authorString = "-Henry Ford"
alert(authorString)

// This code puts authorString at the end of quoteString in order to create the completeString
let completeString = authorString.push(quoteString)
alert(completeString)







